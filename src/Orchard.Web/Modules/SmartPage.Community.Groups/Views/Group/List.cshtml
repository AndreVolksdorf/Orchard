@using Orchard.ContentManagement;
@{
    Model.ContentItems.Classes.Add("content-items");
    Model.ContentItems.Classes.Add("groups");
}
@Display(Model.ContentItems)
@if (Model.ContentItems.Items.Count < 1)
{
    <p>@T("No groups found.")</p>
}