@using Orchard.Security
@{
    var users = (IEnumerable<IUser>) Model.Users;
}

<div class="usersindepartment">
    <h2>@T("Users in department")</h2>

    <ul>
    @foreach (var user in users) {
        <li>@user.UserName</li>
    }
    </ul>
</div>